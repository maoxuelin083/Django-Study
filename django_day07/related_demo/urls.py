from django.urls import path

from related_demo import views

# urlpatterns = [
#     path('add_user/', views.add_users, name="add_user"),
#     path('add_tag/', views.add_tag, name="add_tag")
# ]